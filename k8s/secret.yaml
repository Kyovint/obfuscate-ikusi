apiVersion: v1
kind: Secret
metadata:
  name: obfuscate-ms-secret
  namespace: ikusito
type: Opaque
# IMPORTANT: Change COP_FACTOR and USD_FACTOR to non-obvious values before deploying.
# Use non-round numbers (e.g. 0.000001337) to make reverse-engineering harder.
stringData:
  DB_USER: ikusiAdm
  DB_PASSWORD: "ikusi$postgres2026"
  COP_FACTOR: "0.000001"
  USD_FACTOR: "0.001"
